name: CI
'on': push
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v1
      - name: Branch name
        run: 'echo running on branch ${GITHUB_REF##*/}'
     
